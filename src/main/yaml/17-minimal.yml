incarnation: sapere

network-model: # Network type description
  type: ConnectWithinDistance # Search a class implementing LinkingRule with this name
  parameters: [2] # Use a constructor that can take a single integer number. Deduce other parameters (if any) from the context.

deployments: # Description of where the nodes should be
  - type: Point # Search a class implementing Displacement with this name
    parameters: [1, 2] # As above: use a constructor requiring only parameters deducible from context and two integers
  - type: Point
    parameters: [0, 2]